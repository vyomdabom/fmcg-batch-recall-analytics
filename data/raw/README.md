# data/raw/

Put your generated MESSY Excel files here (e.g. `freshroute_raw_data.xlsx` with 8 sheets, or one file per table). Follow `../data_dictionary.md`, including the deliberate data quality issues. Never edit these after generation — cleaning happens downstream so it's reproducible.
