# data/cleaned/

Cleaned, standardised versions of the raw tables (CSV or xlsx) ready for SQL import / Power BI. Keep a data-quality log documenting every rule you applied: issue → rows affected → fix → rows fixed/excluded.
